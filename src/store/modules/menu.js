// initial state
// shape: [{ id, quantity }]
const state = {
  items: [
    { icon: 'D', title: 'Dashboard', route: '/' },
    { icon: 'T', title: 'Set up Game', route: '/settings' },
    { icon: 'L', title: 'legal stuff', route: '/gnu' },
  ],
};

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
};
